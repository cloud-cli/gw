export default {
  cache: true,
  clearMocks: true,
  collectCoverage: true,
  coverageDirectory: 'coverage',
  maxWorkers: 1,
  preset: 'ts-jest',
  resolver: 'ts-jest-resolver',
  slowTestThreshold: 1,
};
